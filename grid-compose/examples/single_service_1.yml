version: '1.0.0'

services:
  server:
    flist: 'https://hub.grid.tf/tf-official-vms/ubuntu-24.04-full.flist'
    entrypoint: '/sbin/zinit init'
    resources:
      cpu: 1
      memory: 2048
      rootfs: 25600

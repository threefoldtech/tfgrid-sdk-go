version: '1.0.0'

services:
  new_single_vm:
    flist: 'https://hub.grid.tf/omarabdulaziz.3bot/ubuntu-jammy.flist'
    entrypoint: '/sbin/init'
    environment:
      - SSH_KEY=...
    resources:
      cpu: 1
      memory: 512
    node_id: 14
    networks:
      - yggdrasil